```json
"radioGroup": -1
```

Radio group allows buttons to be grouped together so that only one button in the group can be active at a time. Set to -1 for no grouping, or to a positive integer to group buttons together.