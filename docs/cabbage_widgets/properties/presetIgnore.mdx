```json
"presetIgnore": false
```

When set to true, the widget's value will be ignored when saving and loading presets. This is useful for widgets that should not retain their state between sessions.