
```json
"channels": [
                {
                    "id": "gentableStart",
                    "event": "valueChanged",
                    "range": { "min": 0, "max": -1 }
                },
                {
                    "id": "gentableLength",
                    "event": "valueChanged",
                    "range": { "min": 0, "max": -1 }
                }
            ]
```

The `genTable` has two default channels that provide information about the current selection. Note these channels are only valid if `selectableRegions` is set to `true`.