```json
"marker": {
    "thickness": 0.2,
    "start": 0.1,
    "end": 0.9
}
```

Marker defines visual markers on range sliders. thickness sets marker width, start and end define the marker range as normalized values (0-1).