```json
"text": {"x": "X Label", "y": "Y Label"}
```
Sets the text labels for the X and Y axes of the xyPad. Each axis can have its own label displayed in the value boxes at the bottom of the widget. If the labels contain separators like `/`, `|`, or `\`, they will be automatically split during conversion from Cabbage2 format.
