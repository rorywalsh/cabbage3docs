```json
"rotate": {
    "x": 0,
    "y": 0,
    "radians": 0
}
```

Rotate defines rotation transformation for image widgets. x and y set the rotation center point, radians sets the rotation angle in radians.