import Caption from "./properties/caption.mdx";
import Size from "./properties/size.mdx";
import Id from "./properties/id.mdx";
import Visible from "./properties/visible.mdx";
import ZIndex from "./properties/zindex.mdx";
import CodeBlock from '@theme/CodeBlock';
import widgetCode from '!!raw-loader!../../static/examples/checkbox.csd';
import StyleForm from "./properties/form.style.mdx";

# Form

`form` creates the main application window. The default values for size are 600x300. There can only be one form per instrument, and it has the unique channel name of `MainForm`. The `caption` property will only be displayed when running an instrument as a standalone application.

:::note
Configuration properties like `pluginId`, `channelConfig`, `enableDevTools`, `logger`, and `package` have been moved to the top-level of the Cabbage JSON structure. See the [Widget Introduction](./intro.md) for more details.
:::

## Properties (alphabetical)

Below is the full, alphabetical list of top-level widget properties for `form`.


<a id="caption" class="anchor-target"></a>
### Caption
<Caption />

<a id="id" class="anchor-target"></a>
### Id
<Id />

<a id="style" class="anchor-target"></a>
### Style
<StyleForm />

## Example

<CodeBlock language="csound">{widgetCode}</CodeBlock>