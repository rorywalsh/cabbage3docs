```json
"style": {
  "opacity": 1,
  "borderRadius": 4,
  "borderWidth": 1,
  "borderColor": "#dddddd",
  "fill": "#0295cf"
},
```

- When `file` is set, the image is rendered and `fill` is not visible; otherwise a filled rectangle is shown.
- Colors accept named CSS colors, hex (e.g., #RRGGBB or #RRGGBBAA), or rgb/rgba strings.
